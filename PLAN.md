# 点子发芽 MVP 执行计划

## 1. 项目目标

实现一个名为“点子发芽”的个人新知孵化 MVP，用本地 Markdown / JSON / 脚本 / Codex automation 支撑每天的新信息输入、资料整理、旧内容关联、权重建议和晚间复盘。

第一版目标不是产品化，而是形成一个能连续使用的个人工作流：白天轻输入，晚上自动整理，用户手动判断和修正，知识库逐步生长。

必须遵守：

- 不使用 OpenAI API。
- 优先使用本地 Markdown、JSON、脚本和 Codex automation。
- 第一版所有内容都应能被用户手动检查和修改。
- 不追求正式产品化、多用户、商业模式或复杂 UI。
- 不引入数据库、向量库、大型知识图谱或复杂依赖。

## 2. 自用场景

用户每天的自然流程应是：

1. 白天把当天遇到的新关键词、概念、机会、人物、文章或想法写进当天输入文件。
2. 晚上 23:00 由 Codex automation 读取当天输入。
3. automation 基于本地知识库和可用网页资料生成日报。
4. 用户查看日报中的保留、合并、升权、降权、继续追踪建议。
5. 用户直接编辑 Markdown 文件完成最终确认。
6. 重要主题被长期追踪，低价值内容逐渐归档，点子种子可以升级为项目。

设计时要优先保证这个系统能进入日常生活：

- 输入成本低。
- 晚间报告短而有判断。
- 文件结构清晰。
- 用户能随时手动修正。
- 自动化只辅助整理，不替用户做最终决定。

## 3. MVP 范围

第一版只做：

- 本地文件夹结构。
- 每日关键词输入文件。
- 知识节点 Markdown 文件。
- 来源、关系、权重、点子种子的轻量记录。
- 每晚日报模板。
- Codex automation 的任务提示词。
- 少量可选 PowerShell helper 脚本，用于创建当天输入文件和检查结构。

第一版不做：

- OpenAI API 集成。
- Web App。
- 数据库。
- 向量库。
- 复杂知识图谱。
- 多用户系统。
- 商业化功能。

## 4. 推荐仓库结构

在工作目录中创建以下结构：

```text
D:\新知发芽项目\
  PLAN.md
  README.md
  inbox\
    YYYY-MM-DD.md
  library\
    nodes\
    sources\
    seeds\
  reports\
    daily\
  tracking\
    topics.md
  templates\
    daily-input.md
    knowledge-node.md
    source.md
    idea-seed.md
    daily-report.md
  automation\
    nightly-codex-prompt.md
  scripts\
    new-today.ps1
    validate-structure.ps1
```

目录职责：

- `inbox/`：每日输入，保留用户当天写下的原始关键词和备注。
- `library/nodes/`：长期知识节点，包括概念、领域、人物、组织、项目、问题等。
- `library/sources/`：来源记录，包括文章、网页、视频、论文、播客等。
- `library/seeds/`：点子种子，存放尚未成为项目的个人想法。
- `reports/daily/`：每日自动生成的日报。
- `tracking/topics.md`：长期追踪主题清单。
- `templates/`：所有人工可读模板。
- `automation/`：给 Codex automation 使用的提示词。
- `scripts/`：本地辅助脚本。

## 5. 核心数据对象

### 关键词

关键词是当天输入的原始触发词，保留在 `inbox/YYYY-MM-DD.md`。

关键词可以是：

- 新概念。
- 新机会。
- 新兴趣。
- 人物或组织。
- 文章、视频、论文或链接。
- 一句粗糙想法。

关键词不要求一开始写得完整，第一版要允许粗糙输入。

### 知识节点

知识节点是长期存在的新知对象，存放在 `library/nodes/`。

节点类型包括：

- `concept`：概念。
- `domain`：领域。
- `person`：人物。
- `organization`：组织。
- `project`：项目。
- `question`：问题。

知识节点文件使用人工可读格式，不依赖复杂解析：

```text
# 节点标题

ID: node-slug
Type: concept | domain | person | organization | project | question
Weight: 1-5
Status: candidate | active | watch | archived
Last seen: YYYY-MM-DD
Aliases:
Related:
Sources:

## 一句话定义

## 为什么对我重要

## 当前理解

## 近期动态

## 下一步

## 更新记录
```

### 来源

来源是文章、网页、视频、论文、播客或用户提供的材料，存放在 `library/sources/`。

来源文件至少记录：

- 标题。
- URL 或本地路径。
- 类型。
- 访问日期。
- 关联节点。
- 关键摘要。
- 用户判断。

### 权重

权重是节点对用户个人知识系统的重要性和活跃度表达，使用 1-5 分。

权重不是客观价值，也不是搜索热度，而是用户个人注意力分配。

### 关系

关系使用知识节点中的 `Related:` 字段记录，第一版只做文本化关联，不做复杂图谱。

关系可以表达：

- 相似。
- 支撑。
- 冲突。
- 上下游。
- 同领域。
- 可转化为点子。

### 日报

日报是每日复盘入口，存放在 `reports/daily/YYYY-MM-DD.md`。

日报不是资讯简报，而是帮助用户判断：

- 今天哪些东西值得留下。
- 哪些只是噪音。
- 哪些和历史内容有关。
- 哪些主题值得继续追踪。
- 明天可以推进什么小动作。

### 点子种子

点子种子存放在 `library/seeds/`，用于记录尚未成为项目的个人想法。

点子种子不是正式项目，不需要完整商业计划，只需要记录：

- 点子名称。
- 触发来源。
- 关联节点。
- 为什么有意思。
- 可能的最小下一步。
- 当前状态。

## 6. 有新输入时的每日工作流

当 `inbox/YYYY-MM-DD.md` 存在且包含关键词时，automation 执行以下流程：

1. 读取当天关键词和备注。
2. 对每个关键词做基础理解：它是什么、为什么值得注意、和用户有什么可能关系。
3. 搜索资料时优先使用 Codex 可用的浏览能力和用户提供的来源链接，不调用 OpenAI API。
4. 在 `library/nodes/` 中查找同名、别名或语义相近节点。
5. 若已有节点，则提出更新建议，追加到日报中。
6. 若没有节点，则建议创建 candidate 节点。
7. 发现有价值的外部资料时，建议写入 `library/sources/`。
8. 发现有个人想法潜力时，建议写入 `library/seeds/`。
9. 生成当天日报。
10. 权重变更默认只写入日报建议，权重 4 或 5 必须等待用户手动确认。

原则：

- 不要无提示覆盖旧节点内容。
- 不要因为一个关键词就创建大量文件。
- 每个关键词摘要控制在 3-6 行。
- 不确定的信息标注“待确认”。
- 没有可靠来源时不要伪造来源。

## 7. 无新输入时的工作流

当当天没有 `inbox/YYYY-MM-DD.md`，或文件中没有有效关键词时，进入复盘模式。

复盘流程：

1. 读取 `tracking/topics.md`。
2. 读取 `library/nodes/` 中权重 4-5 的核心节点。
3. 读取最近 14 天未更新但状态为 `active` 或 `watch` 的节点。
4. 优先选择 3-5 个节点做轻量复盘。
5. 如可联网，则检查重要主题是否有新进展。
6. 如不可联网，则只基于本地库做旧内容复盘。
7. 生成“无新输入日报”。

无新输入日报应包括：

- 今日无新关键词说明。
- 被复盘的旧主题。
- 是否发现新关联。
- 是否建议继续追踪。
- 是否建议降权。
- 明日一个小动作。

原则：

- 无输入日不是空报告。
- 无输入日也不要变成信息噪音日。
- 不自动创建大量新节点。
- 优先帮助用户重新看见重要旧内容。

## 8. 权重系统第一版规则

权重使用 1-5 分：

- 1：低价值存档，见过即可。
- 2：轻度关注，有意思但暂不投入。
- 3：稳定关注，和长期兴趣有关。
- 4：高优先级，近期反复出现或有行动价值。
- 5：核心主题，值得持续追踪，并可能转成项目、文章、研究或产品。

初始规则：

- 新节点默认权重 2。
- 与已有重要主题有关，可建议权重 3。
- 同一主题 7 天内多次出现，可建议 +1。
- 关联到 2 个以上 active 节点，可建议 +1。
- 30 天无更新且无行动，可建议 -1。
- 权重 4 或 5 必须由用户手动确认。

权重调整原则：

- AI 只提出建议。
- 用户做最终决定。
- 权重变化要在日报中写明理由。
- 不要设计复杂算法。
- 不要自动把节点升为核心主题。

## 9. 日报生成规则

日报文件路径：

```text
reports/daily/YYYY-MM-DD.md
```

日报固定包含：

```text
# 点子发芽日报 YYYY-MM-DD

## 今日输入关键词

## 今日新增理解

## 新建或更新的知识节点

## 与历史内容的关联

## 权重调整建议

## 值得保留的来源

## 点子种子候选

## 明日一个小推进

## 待用户确认
```

日报风格：

- 短。
- 有判断。
- 不写成百科长文。
- 不写成普通资讯简报。
- 每个关键词摘要控制在 3-6 行。
- 每个关键词最多保留 1-3 个高价值来源。
- 必须包含“明日一个小推进”。
- 必须包含“待用户确认”。

## 10. Codex 自动化任务设计思路

创建一个每天 23:00 Asia/Shanghai 运行的 Codex automation，工作目录为仓库根目录。

automation 提示词写入：

```text
automation/nightly-codex-prompt.md
```

提示词要求 automation：

- 不使用 OpenAI API。
- 读取当天 `inbox/YYYY-MM-DD.md`。
- 有输入时执行新知整理流程。
- 无输入时执行旧内容复盘流程。
- 使用本地 Markdown 文件作为主要记忆。
- 必要时使用浏览能力搜索资料。
- 对不确定信息标注“待确认”。
- 不伪造来源。
- 生成日报后停止。
- 不做产品化扩展。
- 不引入数据库、向量库或复杂依赖。
- 文件更新必须轻量、可审查。

如果 goal mode 能创建 automation，则创建；如果当前环境不允许直接创建，则只写好 `automation/nightly-codex-prompt.md`，并在 README 中说明如何手动创建。

## 11. Helper 脚本要求

### `scripts/new-today.ps1`

用途：创建当天输入文件。

行为：

- 使用本地日期生成 `inbox/YYYY-MM-DD.md`。
- 若文件不存在，则从 `templates/daily-input.md` 创建。
- 若文件已存在，则提示路径，不覆盖。

### `scripts/validate-structure.ps1`

用途：检查 MVP 目录和模板是否完整。

行为：

- 检查所有推荐目录是否存在。
- 检查所有模板文件是否存在。
- 检查 `README.md`、`PLAN.md`、`automation/nightly-codex-prompt.md` 是否存在。
- 输出缺失项。
- 不自动修复，避免隐藏问题。

## 12. README 要求

`README.md` 应面向用户本人，说明：

- 这个系统是什么。
- 每天如何输入关键词。
- 晚上如何查看日报。
- 如何手动确认权重、关系和节点更新。
- 无新输入日会发生什么。
- 哪些事情第一版不做。
- 如何运行 helper 脚本。
- 如何理解权重 1-5。

README 不要写成商业介绍，不要使用产品营销语气。

## 13. 模板文件要求

### `templates/daily-input.md`

包含：

```text
# 每日输入 YYYY-MM-DD

## 今日关键词

- 

## 补充备注

## 今天特别想追踪的问题
```

### `templates/knowledge-node.md`

使用第 5 节中的知识节点格式。

### `templates/source.md`

包含：

```text
# 来源标题

ID:
Type: article | webpage | video | paper | podcast | book | other
URL:
Local path:
Accessed:
Related nodes:

## 关键摘要

## 为什么值得保留

## 用户判断
```

### `templates/idea-seed.md`

包含：

```text
# 点子种子

ID:
Status: raw | watching | testing | promoted | archived
Created:
Related nodes:
Sources:

## 触发

## 点子描述

## 为什么有意思

## 可能的最小下一步

## 风险或疑问
```

### `templates/daily-report.md`

使用第 9 节中的日报结构。

## 14. 验收标准

MVP 完成后应满足：

- 仓库结构完整。
- 所有模板文件存在。
- `README.md` 能指导用户开始每天使用。
- 用户可以手动在 `inbox/YYYY-MM-DD.md` 输入 3-5 个关键词。
- automation 提示词足够清晰，能让 Codex 按有输入 / 无输入两种情况工作。
- 日报模板能帮助用户判断保留、合并、升权、降权和继续追踪。
- 示例知识节点格式统一、可手动编辑。
- 示例点子种子格式统一、可手动编辑。
- 权重建议规则简单、可解释、可手动调整。
- 无新输入日有明确复盘流程。
- 不存在数据库、向量库、复杂依赖或产品化结构。

可选验证：

- 运行 `scripts/validate-structure.ps1`，确认没有缺失文件。
- 运行 `scripts/new-today.ps1`，确认能创建当天输入文件且不会覆盖已有文件。

## 15. 未来产品化预留点

现在只保持结构清晰，不实现产品功能：

- 节点 ID 使用稳定 slug，方便未来迁移。
- 节点类型保持固定枚举，方便未来筛选。
- 日报格式固定，方便未来形成时间线。
- 来源单独存放，方便未来做可信度管理。
- 关系字段保持文本化，未来可迁移到图结构。
- 权重只用 1-5，未来可扩展为活跃度、重要性、行动价值等维度。
- automation 提示词独立存放，方便未来替换为服务端任务。

## 16. 明确不做事项

第一版明确不做：

- OpenAI API 集成。
- 数据库。
- 向量库。
- 知识图谱引擎。
- Web UI。
- 移动端。
- 多用户。
- 登录权限。
- 商业模式。
- 产品定价。
- 对外发布。
- 复杂爬虫。
- 自动订阅系统。
- 复杂标签体系。
- 自动替用户做最终判断。
- 大规模重构。
- 复杂依赖。
- 把日报写成资讯简报。
- 为了未来可能产品化牺牲当前自用体验。

## 17. Goal Mode 执行顺序

请 goal mode 按以下顺序执行：

1. 创建推荐仓库结构。
2. 写入 `README.md`。
3. 写入所有模板文件。
4. 写入 `automation/nightly-codex-prompt.md`。
5. 写入 `scripts/new-today.ps1`。
6. 写入 `scripts/validate-structure.ps1`。
7. 创建当天 `inbox/YYYY-MM-DD.md` 示例文件。
8. 创建一个示例知识节点。
9. 创建一个示例点子种子。
10. 运行结构检查。
11. 汇报创建了哪些文件，以及用户明天如何开始使用。

执行时保持 MVP 克制，不要扩展成正式产品。
